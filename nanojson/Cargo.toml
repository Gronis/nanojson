[package]
name = "nanojson"
version = "0.1.0"
edition = "2021"

[features]
default = ["std", "derive"]
std = []
derive = ["nanojson-derive"]

[dependencies]
nanojson-derive = { path = "../nanojson-derive", optional = true }
